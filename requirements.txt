# ============================================================
# Regression DL Pipeline — requirements (Python 3.11.9)
# Install:  pip install -r requirements.txt
# ============================================================

numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.2
seaborn==0.13.2

# Deep learning. Untuk CPU-only (tanpa GPU NVIDIA), install torch via:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# Kalau punya GPU CUDA, cukup baris di bawah ini:
torch==2.5.1

# Tuning, interpretasi, tracking
optuna==4.1.0
lime==0.2.0.1
mlflow==2.18.0

# Untuk menjalankan notebook
jupyter==1.1.1
ipykernel==6.29.5
